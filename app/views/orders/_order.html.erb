<tr>
    <td>
      <%= image_tag item.product.image.tiny %>
    </td>
    <td>
        <%= number_to_currency((item.item_price_cents / 100) * item.quantity) %>
    </td>
    <td>
      <h4><%= item.quantity %></h4>
      <br>
      <p><%= item.product.description %></p>
    </td>
</tr>
